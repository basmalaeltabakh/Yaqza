# ============================================================
# Yaqza | يقظة — Python Dependencies
# Install: pip install -r requirements.txt
# Python version: 3.9+
# ============================================================

# ── Core Data Science (Everyone) ────────────────────────────
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
scikit-learn>=1.2.0
scipy>=1.10.0
jupyter>=1.0.0
jupyterlab>=3.6.0

# ── ML / Deep Learning (M2) ─────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0
pytorch-forecasting>=1.0.0
xgboost>=1.7.0
tqdm>=4.65.0

# ── ONNX / Edge Inference (M3) ──────────────────────────────
onnx>=1.14.0
onnxruntime>=1.15.0

# ── API & Serving (M3) ──────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
httpx>=0.24.0

# ── Azure SDK (M1, M3, M4) ──────────────────────────────────
azure-iot-device>=2.12.0
azure-ai-ml>=1.8.0

# ── IoT / Messaging (M1) ────────────────────────────────────
paho-mqtt>=1.6.1

# ── MLOps & Experiment Tracking (M4) ────────────────────────
mlflow>=2.5.0

# ── Testing (Everyone) ──────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
